const hours = {
      test1: 18,
      teste2: 20,
      teste3: 22
    };

// hours.forEach((day, index) => {
//   console.log(day % 12)
// });

const lista = {}
hours.forEach(element => {  
  lista[day] = day % 12
});